*** Settings ***
Library         OperatingSystem
default tags    regression

*** Variables ***
${UNIT TEST DIR}=    ${CURDIR}${/}..${/}utest

*** Test Cases ***
Execute unit tests
    ${rc}  ${output}  Run And Return Rc And Output  nosetests ${UNIT TEST DIR}
    Should Be Equal As Integers  ${rc}  0   ${output}