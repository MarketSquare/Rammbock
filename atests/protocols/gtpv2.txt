*** Settings ***
Test Setup      UDP Server and Client are initialized in port ${GTP_CONTROL_PORT}
Test Teardown   Close Connections
Default Tags    regression
Library         rammbock.Rammbock
Resource        ../resources/Messaging.txt
Resource        ../protocols/gtp/v2.txt

*** Variables ***
${GTP_CONTROL_PORT}  2123

*** Test Cases ***
Send and Receive Create Session Request and check that it exist.
    Create and send Create Session Request
    Receive Create Session Request and Validate that Data is not empty

Send and Receive Create Session Request and validate the message
    [Tags]  ${EMPTY}
    Create and send Create Session Request
    Receive Session Request and Validate Message

Make GTPv2 Attach
    [Tags]

*** Keywords ***
Create and send Create Session Request
    Create Message
    Set Create Session Request
    Client Sends Data

Receive Create Session Request and Validate that Data is not empty
    ${message}=  Server Receives Data
    Should not be empty  ${message}

Receive Session Request and Validate Message
    Server Receives Data
    Validate Create Session Request

