*** Settings ***
Library         ../src/Rammbock.py

*** Keywords ***
Try to Use interface which don't exist
    ${EXCEPTION}  Run Keyword And Expect Error  *  Use Interface  WRONG_INTERFACE  foo
    Set Test Variable  ${EXCEPTION}

Exeption '${expected_exception}' should occure
    Should be Equal  ${EXCEPTION}  ${expected_exception}

interface is up
    [Arguments]  @{ifname}
    ${interface_state}=  is_interface_up  @{ifname}
    Should Be True  ${interface_state}

